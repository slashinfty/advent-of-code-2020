'use strict';

module.exports = {
    name: 'dayXX',
    exe: () => {
        const input = require('fs').readFileSync(require('path').join(__dirname, '../inputs/dayXX.txt'), 'utf-8').split('\n').filter(Boolean);
        // Test
        // const input = 
        // Part one test answer: 
        // Part two test answer: 

        // Part one: 

        // console.log('Part One: ' + );

        // Part two: 

        // console.log('Part Two: ' + );
    }
}